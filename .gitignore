# Fresh build output
_fresh/

# Dev / OS
.DS_Store

# Local secrets — NUNCA commitar
.env
.env.local

# Logs/runtime
nohup.out
*.log

# Assets servidos via CDN (repo separado Iuri07/brasileirao-fantasy-assets).
# Em dev é um symlink pro repo de assets; em prod chega via jsDelivr.
static/atletas
static/escudos
static/players
static/times_escudos
static/fonts


# Claude Code local settings (não compartilhado)
.claude/settings.local.json
